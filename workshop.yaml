name: peberanek-github-io
base: ubuntu@24.04
sdks:
  - name: opencode
  - name: uv
    slots:
      mkdocs:
        interface: tunnel
        endpoint: localhost:10020        # MkDocs inside the workshop
  - name: system
    plugs:
      mkdocs:
        interface: tunnel
        endpoint: localhost:10020        # port on your host
actions:
  sync: uv sync
  upgrade: uv lock --upgrade
  serve: NO_MKDOCS_2_WARNING=1 uv run mkdocs serve --livereload --dev-addr 127.0.0.1:10020
